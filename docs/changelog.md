# История изменений

## 2026-09-13 (вечер) — Усложнение вопроса о себестоимости, устранение дублей вариантов, диагностика email
- По жалобе пользователя вопрос id 38 ("себестоимость + маржа = тариф", с
  явно неверным отвлекающим вариантом "тариф не зависит от себестоимости")
  признан слишком лёгким и заменён на расчётный вариант (маржа считается как
  % от тарифа, а не от себестоимости — требует реального расчёта)
- Добавлено ещё 5 новых вопросов в `src/data/questions.js` (id 46–50): три
  вариации на тему себестоимость/маржа (несколько статей расходов, сравнение
  с рыночным тарифом, обратная задача "найти маржу по тарифу и себестоимости",
  ещё одна обратная задача "найти себестоимость по тарифу и марже"), плюс один
  вопрос по таможенным понятиям (СВХ) — банк вырос с 45 до 50 вопросов
- По требованию пользователя пересобрана структура `VARIANTS` в
  `src/public/js/test.js`: раньше 5 id (23, 24, 25, 32, 41) намеренно
  дублировались между двумя вариантами теста (50 позиций на 45 вопросов
  банка); теперь дублей нет вообще — 2 варианта по 25 вопросов покрывают
  ровно 50 из 50 вопросов банка без единого пересечения (проверено
  программно: `duplicates: []`, `missing: []`)
- Изменения проверены (`node --check`, программная проверка покрытия), но
  **не закоммичены** — остались в рабочей копии на конец сессии
- Разобрана жалоба на недоставку письма с результатом теста на
  `kpv@es-trans.pro`: результат сохраняется на диске независимо от почты
  (`data/results/1789301102551.json` подтверждён); `RESEND_API_KEY` пуст →
  используется SMTP fallback; из dev-контейнера SMTP авторизация и реальная
  тестовая отправка прошли успешно и дошли до адресата — значит проблема,
  вероятно, в блокировке SMTP-порта именно на проде (как предупреждает
  CLAUDE.md), но пользователь ещё не подтвердил это повторным прохождением
  теста на самом домене
- Обнаружено (не устранялось по просьбе пользователя — "пароль не трогать"):
  в `.env` в открытом виде лежит реальный пароль от `test@es-trans.pro`
  (`SMTP_PASS`); файл не в git, но с этим типом утечки на проекте уже был
  инцидент — см. `docs/state.md` и ссылку в CLAUDE.md

## 2026-09-13 — Углубление банка вопросов (кейсы, расчёты) и деплой
- По запросу сотрудника (вопросы показались поверхностными по сравнению с
  курсом) переработан банк вопросов `src/data/questions.js` без изменения
  технического формата теста (1 вопрос → 4 варианта → 1 правильный),
  чтобы не трогать движок теста
- Точечно (10 вопросов) переведены в кейсовый/расчётный формат вместо прямых
  вопросов-определений: расчёт объёмного веса для авиатарифа (/6000), расчёт
  компенсации по CMR (8,33 СПЗ/кг), расчёт тарифа (себестоимость + маржа),
  кейс про обязанность платить по системе «Платон», кейсы на определение
  категории груза (тяжеловесный/длинномерный/опасный) по конкретным
  параметрам, «найди нарушение» (контрабанда на границе), ситуационные кейсы
  на ответственность (крепление груза, ошибка экспедитора)
- Углублены темы Инкотермс (конкретное сравнение момента перехода риска по
  EXW и DAP, а не общее определение), CMR-страхование, книжка МДП
- Выровнена длина формулировок вариантов ответа во всех вопросах, где
  правильный вариант ранее был статистически длиннее и подробнее остальных
  (угадывание без знания материала)
- Пересчитано и выправлено распределение правильных ответов по позициям
  А/Б/В/Г: было 11/14/14/6 (Г сильно занижен), стало 11/10/12/12 — ровно,
  как того требует правило CLAUDE.md
- ID вопросов и структура `VARIANTS` в `src/public/js/test.js` не менялись —
  оба варианта теста по-прежнему покрывают весь банк из 45 вопросов без
  пропусков (проверено программно)
- Обновления проверены через headless Chromium (Playwright, временно
  установлен и удалён): пройден прогон теста, вживую увидены новые кейсовые
  вопросы (расчёт объёмного веса, расчёт тарифа, кейс по опасным грузам,
  кейс по массе груза), ошибок в консоли и на сервере не обнаружено
- Изменения закоммичены (`1f72c8d`) и задеплоены через `vibe-deploy-service`
  (ответ 200, `firstRun: false`, порт 3100); визуальное подтверждение на
  самом домене пользователем ещё не получено (прямой curl-доступ к домену
  с рабочего контейнера заблокирован файрволом — это ожидаемо)

## 2026-09-12 — Синхронизация с GitHub, новая структура вариантов, деплой и брендинг
- Связан локальный git-репозиторий с существующим GitHub-репозиторием
  `krutko77/transport-logistics-course` (HTTPS + сохранённые credentials,
  SSH-ключ не подошёл к нужному аккаунту)
- При синхронизации сохранены наши осознанные кастомизации (брендированный
  `mailer.js`, актуальные `CLAUDE.md`/`project-brief.md`) поверх версии из
  репозитория; удалены ненужные zip-архивы курса (~9.5 МБ)
- Восстановлена реальная структура 5 тематических блоков курса из исходных
  .docx-файлов (блок 1: id 1–10, блок 2: id 11–20, блок 3: id 21–27,
  блок 4: id 28–36, блок 5: id 37–45) — нигде ранее не была явно закодирована
- Реализована и затем изменена по требованию пользователя система вариантов
  теста: сначала 3 варианта по 25 вопросов (по 5 из каждого блока), потом
  переделано на 2 варианта по 25 вопросов с полным суммарным покрытием всех
  45 вопросов банка (проверено программно: union=45, missing=[])
- Добавлена динамическая рандомизация порядка вариантов ответа (А/Б/В/Г) на
  клиенте при каждом показе вопроса (`shuffleOptions()`); изменён контракт
  `POST /api/results` — сравнение ответов по тексту (`answerTexts`), а не по
  индексу, так как позиция ответа больше не фиксирована
- Задеплоено на `https://test-logistics.es-trans.ru/` через
  `vibe-deploy-service` — после того как администратор портала освободил
  домен от старого конфликтующего проекта; SSL выпущен автоматически при
  первом деплое
- Добавлен брендинг ЕС Транс: логотип (`logo.svg`) и favicon перенесены из
  проекта `es-trans-test-OM` в шапку сайта вместо текстового заголовка
- Уменьшена толщина красной линии под шапкой с 3px до 2px (по требованию)
- Акцентный цвет проекта сменён с красного (`#CC0000`) на синий (`#0098DD`) —
  кнопка, обводка полей/вариантов, линия под шапкой — по образцу, присланному
  пользователем

## [Дата] — Приложение собрано и готово к деплою
- Собран банк из 45 вопросов (конвертация из открытых экзаменационных вопросов
  на основе конспектов курса), правильные ответы сбалансированы по А/Б/В/Г (12/11/11/11)
- Собрана система из 5 вариантов теста по 25 вопросов, покрывающих все 5 блоков
- Построен весь каркас приложения (server.js, results.js, mailer.js, test.js,
  index.html, style.css) по паттерну проекта es-trans-test-OM
- Проведён и пройден локальный smoke-тест (сервер, отдача вопросов, приём результатов)
- Добавлено правило работы на русском языке в CLAUDE.md
- Скопирована папка `.claude` (команды `/log`, `/decision`, `/handoff`, `/end`) из es-trans-test-OM
- Создана инфраструктура журналов (`docs/state.md`, `docs/decisions.md`,
  `docs/handoff.md`, `docs/last-session.md`, `scripts/snapshot.sh`)
- Инициализирован git-репозиторий, сделан первый коммит
- Исправлена ошибка в `.gitignore`: правило `data/` случайно исключало
  `src/data/questions.js`, заменено на `/data/`

## [Дата] — Старт проекта
- Создан проект из шаблона _base
- Проведён онбординг, заполнен project-brief.md
