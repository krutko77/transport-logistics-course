# Состояние проекта

**Последнее обновление:** 2026-09-20

## Где мы сейчас
Проект задеплоен и работает: `https://test-logistics.es-trans.ru/`.
Банк вопросов расширен до 54 (добавлен блок 6 — габариты/масса ТС и
ответственность при погрузке), несколько формулировок переписаны по
просьбе пользователя. Шапка приведена к стилю `test-2.es-trans.ru`
(фон `#F5F5F5`, нижняя граница `1px solid #4A4A48`).

**2026-09-20: обнаружен и исправлен баг доставки писем.** Пользователь
прошёл тест — письмо на `kpv@es-trans.pro` не пришло. Диагностика прямым
curl к UniSender Go показала: передача `apiKey`/`user_id` в теле JSON (как
было реализовано в `src/services/mailer.js` и задокументировано в
CLAUDE.md) возвращает `code 101: API key is missing`. Ключ нужно
передавать в заголовке `X-API-KEY`; `user_id` при этом не требуется.
Исправлено в `mailer.js` и в CLAUDE.md, задеплоено. **Предыдущее
"подтверждено эмпирически через тестовый запрос в личном кабинете
UniSender" (см. историю ниже) было ошибочным или устарело** — вывод не
проверялся повторно с момента первого внедрения. Само письмо, отправленное
диагностическим curl-запросом, ушло успешно (`status: success`) — стоит
уточнить у пользователя, дошло ли оно и дошло ли обычное письмо через
починенный `mailer.js` после деплоя.

**2026-09-20: тот же баг исправлен и в соседних проектах.** Тот же паттерн
"ключ в теле JSON" был скопирован в `es-trans_test_GFD_MOO` (test-1) и
`es-trans_test_standard_MOO` (test-2) — исправлен там же (`mailer.js` +
CLAUDE.md), оба проекта задеплоены. `es-trans_test_GFD_MOO` — гит-репозиторий
без настроенного remote (закоммичено локально, пуш не требуется/невозможен).
`es-trans_test_standard_MOO` — не гит-репозиторий вовсе, изменения только на
диске, подхвачены деплоем через rsync. Сквозная проверка (реальное
прохождение теста → письмо руководителю) в этой сессии не делалась ни для
одного из трёх проектов — стоит попросить кого-то пройти каждый тест и
подтвердить получение письма.

Конфликт доменов `test-1`/`test-2` у соседних проектов
`es-trans_test_GFD_MOO` и `es-trans_test_standard_MOO` снят
администратором портала 2026-09-19.

## Сейчас в работе
- **2026-09-20: НОВОЕ — реальные письма с результатами теста не приходят
  ВООБЩЕ, даже в спам.** Отличается от истории ниже: диагностические
  curl-запросы напрямую к UniSender Go доходили (пусть и в спам), но при
  реальном прохождении теста через сайт письмо не появляется нигде.
  Добавили временный эндпоинт `GET /api/debug-env` (только true/false по
  ключевым переменным, без раскрытия секретов) — задеплоили, пользователь
  открыл в браузере (curl с этого контейнера на свой домен зависает,
  известный анти-loopback файрвол). **Результат: ВСЕ переменные окружения
  на сервере пустые** — `MANAGER_EMAIL`, `UNISENDER_GO_API_KEY`,
  `UNISENDER_GO_USER_ID`, `UNISENDER_GO_API_URL`, `UNISENDER_GO_FROM` все
  `false`. Повторный деплой через `vibe-deploy-service` не исправил —
  переменные остались пустыми после повторной проверки. Эндпоинт
  `/api/debug-env` удалён из кода после диагностики (это был временный
  диагностический код, не оставлять в проде).
  **Вывод:** `.env` проекта либо не копируется на сервер при rsync-деплое,
  либо pm2-процесс запускается без него — код `results.js` и
  `mailer.js` при этом работает правильно (проверено ранее прямым curl
  с реальными значениями из `.env`), просто на сервере ему неоткуда взять
  значения. `if (process.env.MANAGER_EMAIL)` в `results.js` в этом случае
  молча пропускает отправку письма без единой ошибки — отсюда и полное
  отсутствие письма даже в спаме.
  **Это инфраструктурная проблема `vibe-deploy-service`, не код проекта.**
  Агент не может её починить без SSH-доступа к серверу — нужно обратиться
  к администратору портала с просьбой проверить, копируется ли `.env`
  файла проекта при деплое и передаётся ли он в pm2-процесс.

- **2026-09-20 (более ранняя часть той же сессии): письма уходят, но
  попадают в СПАМ — причина найдена.**
  Сотрудник прошёл тест повторно — письмо снова не появилось во
  «Входящих» на kpv@es-trans.pro. Диагностика:
  1. Прямой curl к UniSender Go API с текущим ключом отработал успешно
     (`status: success`) — API-контракт (`X-API-KEY` в заголовке) исправен,
     не является причиной.
  2. Проверили личный кабинет UniSender Go: домен отправки `es-trans.pro`
     — подтверждён, DKIM настроен. Домен ссылок `unisender.es-trans.pro`
     — в статусе "Настроен". Оба не являются причиной.
  3. Письмо нашлось в папке СПАМ на kpv@es-trans.pro. Разобрали заголовки
     (`X-Spam-Report` от Rspamd на стороне timeweb): `SPF`, `DKIM`, `DMARC`
     все проходят (`R_SPF_ALLOW`, `R_DKIM_ALLOW`, `DMARC_POLICY_ALLOW`).
     **Главный вклад в спам-балл (10.4, порог обычно ~7) —
     `URIBL_BLACK(7.50)`**: один из URL в письме (вероятнее всего ссылка
     `List-Unsubscribe` на `unisender.es-trans.pro` или инфраструктурный
     домен UniSender `themailer.ru`/`msgpanel.com` в цепочке `Received`)
     находится в чёрном списке URIBL, который использует почтовый сервер
     timeweb. Остальные символы (`BAYES_SPAM 1.60`, `CTYPE_MIXED_BOGUS
     1.00`, `FORGED_SENDER 0.30` и т.д.) вносят по мелочи и сами по себе
     ниже порога.
  **Вывод:** это не баг кода проекта и не настройка UniSender — это
  занесение домена/URL в чёрный список на стороне почтового сервера
  получателя (timeweb). **Дальнейшие действия — вне зоны агента:**
  пользователь договорился написать администратору timeweb/почты
  `es-trans.pro` с просьбой добавить в белый список (whitelist) домены
  `unisender.es-trans.pro`, `themailer.ru`, `msgpanel.com`, либо
  скорректировать/отключить правило `URIBL_BLACK` для писем от
  `noreply@es-trans.pro`. Приложить пример заголовков письма (см. выше)
  как доказательство. Код и деплой проекта менять не требуется.
  **2026-09-20, попытка исправить со стороны кода — частично неудачная.**
  Добавили в `mailer.js` `skip_unsubscribe: 1` и `track_links: 0`,
  задеплоили. Пользователь прошёл тест — письмо не пришло вообще (даже в
  спам). Диагностика прямым curl с теми же параметрами показала:
  `skip_unsubscribe` вызывает `code 1588: The user must have the
  'allow_skip_unsubscribe' flag set` — этот флаг аккаунту UniSender Go не
  разрешён, запрос целиком отклоняется с ошибкой, письмо не уходит.
  **Исправлено:** убрали `skip_unsubscribe` из `mailer.js`, оставили
  только `track_links: 0` (он один отработал успешно при прямой проверке
  curl — `status: success`). Заодно устранили более фундаментальную
  проблему процесса: раньше ошибка отправки письма молча проглатывалась
  (`try/catch` в `results.js` только логировал в `console.error`, который
  недоступен без SSH) — фронт всегда писал "отправлены руководителю"
  независимо от реального результата. Теперь `results.js` возвращает
  `emailSent`/`emailError` в JSON-ответе, а `test.js` показывает честное
  сообщение ("письмо не отправлено", если `emailSent: false`) — это
  сильно ускорит диагностику подобных проблем в будущем, не полагаясь на
  ручные curl-проверки и просьбы к пользователю смотреть спам.
  Нужно задеплоить и проверить ещё раз: пройти тест → проверить сообщение
  на экране результата и почту (Входящие + СПАМ).
- **Cooldown между попытками (8 часов) временно отключён** по просьбе
  сотрудника — `getCooldownUntil()` в `src/public/js/test.js` сразу
  возвращает `null`, сама логика не удалена (закомментирована ниже
  `return`). Нужно вернуть обратно, когда сотрудник попросит.
- Подготовлена методичка для обучения сотрудника системе
  `commands + docs` (три файла в
  `/home/my_workspace/onboarding-commands-guide/`, вне этого проекта) —
  завершена, не требует продолжения в этом проекте.
- Все известные хвосты этой сессии закрыты (см. ниже).

## 2026-09-21: правки вопросов по итогам обратной связи сотрудника, задеплоено
- **id 31** — вопрос переформулирован (был про максимальную массу седельного
  тягача, дублировал по смыслу другой вопрос про массу) на "Какая внутренняя
  длина по полу кузова у стандартного тентового полуприцепа грузоподъёмностью
  24 тонны?" — верно 13,6 м (позиция В сохранена).
- **id 46** — исправлена ошибка в `correct` (расчёт тарифа из топлива/
  амортизации/зарплаты/маржи 15%): было указано неверное "45 500 руб.",
  верно "46 000 руб." (40 000 + 15% = 46 000).
- **id 48** — та же категория ошибки: маржа при тарифе 55 000/себестоимости
  44 000 — было указано "20%", верно "25%" ((55000-44000)/44000).
- Оба случая (id 46, id 48) — это не опечатки в вариантах ответа, а неверный
  индекс `correct`: правильный вариант уже присутствовал в `options`, просто
  был не помечен как правильный. Стоит перепроверить остальные расчётные
  вопросы блока "Себестоимость и тарифы" на всякий случай — не делалось
  в этой сессии.
- **Вопросы 25 (вариант 1) / 24 (вариант 2) поменяны местами** между
  вариантами по просьбе сотрудника: id 45 (сертификация без сертификата
  соответствия) теперь в варианте 1, id 48 (маржа 55000/44000) — в
  варианте 2. Оба варианта по-прежнему покрывают все 54 вопроса банка без
  пересечений. См. `src/public/js/test.js`.
- Задеплоено через `vibe-deploy-service`, `firstRun: false` (обычное
  обновление, не первый деплой).

## 2026-09-21: вторая партия правок вопросов по обратной связи, задеплоено
- **id 42** — "в фуре" → "в грузовом отсеке" (нейтральная формулировка).
- **id 11** — "в РБ и РФ" → "в ЕАЭС" (значение ширины не менялось).
- **id 51** — "в России" → "в ЕАЭС" (значение высоты не менялось).
- Стартовый экран (`src/public/js/test.js`) — захардкоженный текст
  "25 вопросов" исправлен на "27 вопросов" (не совпадал с реальным числом
  вопросов в варианте после более раннего расширения банка/вариантов;
  стоит иметь в виду на будущее — при следующем изменении числа вопросов
  в варианте не забыть про эту строку, она не считается автоматически).
- Задеплоено через `vibe-deploy-service`, `firstRun: false`.

## 2026-09-21: cooldown возвращён (9 часов), защита от копирования вопросов
- **Cooldown между попытками включён обратно** (был временно отключён по
  просьбе сотрудника, см. историю ниже) — интервал изменён с 8 на 9 часов.
  `getCooldownUntil()` в `src/public/js/test.js` больше не возвращает `null`
  сразу, работает полная логика.
- **Копирование текста вопросов/вариантов ответа заблокировано** —
  `user-select: none` на `.question-text`/`.option__text` в
  `src/public/css/style.css`, плюс блокировка `oncontextmenu`/`oncopy` на
  этих элементах в `renderQuestion()` (`src/public/js/test.js`). Это
  клиентская защита, не абсолютная — через DevTools/просмотр исходного
  кода текст всё равно доступен, это стандартное ограничение любого
  фронтенд-решения такого рода, не баг.
- Задеплоено вместе с правками вопросов id 42/11/51 (см. запись выше) и
  исправлением текста "27 вопросов" на стартовом экране.

## Ожидание внешних действий (2026-09-21)
- **Письмо с результатами теста подтверждённо попадает в СПАМ** (скриншот
  пользователя: письмо "Тест-8 — 11/27 (41%)" лежит в папке СПАМ на
  kpv@es-trans.pro) — проблема `URIBL_BLACK` из истории ниже всё ещё
  актуальна, воспроизводится на новых письмах.
- Пользователь написал в техподдержку UniSender Go с просьбой включить
  флаг `allow_skip_unsubscribe` для аккаунта (без него `skip_unsubscribe: 1`
  в `mailer.js` вызывает `code 1588` и ломает отправку — см. историю ниже).
  **Ответ от бота техподдержки (2026-09-21):** подтвердили, что запрос
  оформлен верно, требуется ручное включение флага саппортом — ждём.
  **Уточнение от живого оператора UniSender (Владислав, 2026-09-21):**
  флаг `allow_skip_unsubscribe` включат не раньше чем через **2 недели
  активных рассылок** (аккаунт слишком новый, статистику проверить не
  могут). При этом Владислав прямо написал: **наличие ссылки отписки само
  по себе НЕ является триггером спам-фильтра** — это меняет приоритет
  расследования: причина `URIBL_BLACK`, скорее всего, не в
  `unisender.es-trans.pro` (ссылка отписки), а в инфраструктурных доменах
  UniSender в цепочке `Received` (`themailer.ru`/`msgpanel.com`), на
  которые повлиять с нашей стороны нельзя вообще. Ждать 2 недели ради
  `skip_unsubscribe` может не иметь смысла — решение по-прежнему
  вероятнее со стороны timeweb (whitelist/корректировка правила). Как
  временный вариант можно кастомизировать сам блок отписки через
  `{{UnsubscribeUrl}}` (https://godocs.unisender.ru/unsubscribe-link#customizing),
  но это не убирает домен ссылки, а значит вряд ли повлияет на
  `URIBL_BLACK`.
- Параллельно подготовлен (агентом, для отправки самим пользователем)
  текст письма администратору почты es-trans.pro/timeweb с просьбой
  добавить в whitelist домены `unisender.es-trans.pro`, `themailer.ru`,
  `msgpanel.com` либо скорректировать правило `URIBL_BLACK` для
  `noreply@es-trans.pro`. Статус отправки/ответа — уточнить у пользователя.

## Следующие шаги
1. ~~Передать администратору решение про `pm2 delete es-trans-test`~~ —
   передано пользователем администратору напрямую.
2. Проверить, актуален ли ещё `docs/admin-request-rename-project-slug.md`
   — конфликт слагов, который он описывает, администратор уже решил;
   возможно, файл нужно пометить закрытым или удалить. (Не тронут в этой
   сессии — отдельная задача.)
3. ~~Закоммитить и запушить в `origin/main`~~ — сделано, `origin/main`
   теперь на `b78d79f`.
4. ~~Решить открытый вопрос по `.env`/`.env.example`~~ — сделано:
   неиспользуемые `SMTP_*` убраны из обоих файлов, `.env.example`
   приведён в соответствие с UniSender Go.
5. ~~Реальный пароль `SMTP_PASS` в `.env`~~ — строка удалена из `.env`
   (файл не в git). Сам пароль на стороне timeweb/`test@es-trans.pro`
   не отзывался — по решению пользователя это не требуется, раз он
   больше нигде не хранится в открытом виде.

## История (сжато, детали — в changelog.md и decisions.md)

- **UniSender Go подключён** взамен Resend (юридический риск по
  152-ФЗ/242-ФЗ для ПДн сотрудников за рубежом, см. `docs/decisions.md`).
  Паттерн перенесён также в `es-trans_test_standard_MOO` и
  `es-trans_test_GFD_MOO` — код, `.env`/`.env.example`, `package.json`,
  а затем и `CLAUDE.md`/`docs/*` этих проектов приведены в соответствие
  (Resend/SMTP убраны). Домен ссылок (tracking domain) обязателен — без
  него `code 229`.
  **Уточнение 2026-09-20:** ключ передаётся в заголовке `X-API-KEY`, а
  НЕ в теле JSON — см. запись выше про исправленный баг доставки писем.
- **Инцидент:** реальный API-ключ Resend однажды попал в открытом виде в
  `docs/*.md` (не в `.env`) и был заблокирован GitHub push protection.
  Ключ отозван, история переписана. Вывод: секреты нельзя вставлять в
  markdown даже как историческую справку.
- **Конфликт доменов** `test-1`/`test-2.es-trans.ru` возник из-за
  переименования папок проектов на диске (`es-trans-test-OM` →
  `es-trans_test_GFD_MOO`, `es-trans-standard` → `es-trans_test_standard_MOO`)
  без соответствующего обновления `registry.json` на портале —
  `vibe-deploy-service` матчит проект по имени папки. Решено
  администратором портала 2026-09-19 (переключение nginx-вхоста на новый
  pipeline для test-1, переименование ключа в registry для обоих).
- Деплой — только через `vibe-deploy-service`
  (`curl -X POST http://172.30.0.1:8191/deploy -d '{"project": "<имя_папки>"}'`),
  никогда по SSH (файрвол намеренно блокирует прямой доступ с контейнера
  на хост — это ожидаемое поведение, не повод искать обход).
